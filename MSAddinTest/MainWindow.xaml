<Window x:Class="MSAddinTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MSAddinTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="5" Grid.Row="0" Orientation="Horizontal">
            <Button Padding="2" Margin="2" x:Name="LoadPluginBtn" ToolTip="加载插件">loadPlugin</Button>
            <Button Padding="2" Margin="2" x:Name="UnloadPluginBtn" ToolTip="卸载插件">unLoadPlugin</Button>
            <Button  Padding="2" Margin="2" x:Name="TestBtn" ToolTip="执行插件">test</Button>
        </StackPanel>
    </Grid>
</Window>
